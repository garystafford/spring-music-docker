iptables -t nat -L -n
sudo iptables -L -v
ip route
ip addr

lsof -i tcp; lsof -i udp;
lsof -i :80

weave ps
weave config
weave status

brctl show
brctl showstp docker0

netstat -nat |grep -v CLOSE_WAIT
netstat -tupln
netstat -anp | grep LISTEN
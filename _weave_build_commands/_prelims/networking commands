sudo iptables -t nat -L -n
sudo iptables -L -v

ip route
ip addr

lsof -i tcp; lsof -i udp;
lsof -i :80

weave ps
weave config
weave status
weave status dns
weave status peers

brctl show
brctl showstp docker0

sudo netstat -nat |grep -v CLOSE_WAIT
sudo netstat -tupln
sudo netstat -anp | grep LISTEN